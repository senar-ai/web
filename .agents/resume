#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

# This repository has no persistent backing services or tunnels to repair.
echo "Orb resume complete; no services require restart."
